# zik-backend (P1)

System-level HTTP backend. Runs as uid `zik` in production; in the demo target
it runs as the developer user.

M0.1 scope: `/api/health` only. Session/CSRF middleware lands in M0.2.
